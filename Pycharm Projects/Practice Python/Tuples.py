# Tuple is quite similar with List but some key differences like
# we can't modify th elements because it's immutable;

shakil = (6, 8, "shakil")
shakil2 = [(6, 8, "shakil"), (23, "Khan")] # and we also can store the value like this

print(type(shakil))
print(shakil2.clear())
